package com.directi.training.ocp.example;
/**
 * This enum contains all shapes types
 * For now, the available shapes are circle and square
 * But when the project gets bigger, we might add other types to work on, say rectangles, triangles, ...
 * In this case, we will have to change the code of this enum
 * leading to possible bugs that may break the application.
 * @author yosra
 *
 */
public enum ShapeType
{
    CIRCLE, SQUARE
}
